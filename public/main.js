/*import { usuarioController } from "../server/src/Controllers/usuarioController";

const url = "http://localhost:3000/api";

function getUser() {
    axios.get(url)
    .then(response=>{
        const data = response.data;
        renderResults.textContent = JSON.stringify(data)
    })
    .cath(error=>console.log(error))
}

usuarioController.insertUser(new AxiosUser().axiosIns(values))*/